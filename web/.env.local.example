# For local dev — point to your service account key file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/gcp-sa-key.json

# OR paste the JSON content directly (used on Vercel):
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"soy-sauce-tracker",...}
